// http-server -p 8000
// horizontalSpikesStatics(null)

// verticalSpikesTransition(10 pixeles);

// caidaPajaro(10 pixeles);

// elevacionPajaro(15 pixeles);

// aparicionMoneda(integer / score);

// pajaroSurvivedVerticalSpikes(sumaPuntos, is_left);

// pajaroChocaSpikesVertical(pajaroMuere, reiniciarContador, pajaroHomePosition);

// pajaroChocaSpikesHorizontal(pajaroMuere, reiniciarContador, pajaroHomePosition);

// pajaroGetCoin(pajaroSumaPunto);

// pajaroSumaPunto(contador++);

// pajaroCambiaDireccion(is_left);

// pajaroMuere(contadorReiniciar);

// reiniciarContador(reiniciar);

// pajaroHomePosition(x, y, contador);
